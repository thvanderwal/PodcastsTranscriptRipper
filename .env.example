# Environment Variables for Vercel Deployment

# Apple API Bearer Token (REQUIRED)
# This token is required to access Apple's podcast transcript API
# Tokens expire every 30 days and need to be refreshed
# See README.md for detailed instructions on how to obtain this token
APPLE_BEARER_TOKEN=your_bearer_token_here

# Example:
# APPLE_BEARER_TOKEN=eyJhbGciOiJFUzI1NiIsInR5cCI6IkpXVCIsImtpZCI6IldlYlBsYXlLaWQifQ...

# User-Agent for Apple API requests (OPTIONAL)
# Default: Podcasts/1.1.0 (Macintosh; OS X 15.5)
# Update this if the default User-Agent becomes outdated or if Apple requires a different version
# APPLE_USER_AGENT=Podcasts/1.2.0 (Macintosh; OS X 16.0)

# CORS Allowed Origins (OPTIONAL)
# Comma-separated list of allowed origins for CORS
# If not set, all origins are allowed in development, restricted in production
# Example: https://yourapp.vercel.app,https://www.yourapp.com
# CORS_ALLOWED_ORIGINS=
